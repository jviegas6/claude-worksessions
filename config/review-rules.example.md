# Weekly review — house rules

<!--
  Copied to <work root>/_config/review-rules.md by install.sh. The weekly-review
  skill reads it before proposing anything; where it is more specific than the
  skill, it wins. Keep it to rules — decisions made week by week live in
  _audit/review/ledger.json.
-->

## Default task

- Everything without a ticket goes to **<Default task name>** — team dailies,
  all-hands, 1:1s, tooling and setup included. Never propose `(ignore)` unless asked.

## Epics that catch a programme's work

- Everything related to **<Programme>** goes on the epic `<PROJ-100>` — calls,
  standups, investigations — unless it is a specific task with its own ticket
  (e.g. `<PROJ-144>`, <what it is>).

## Tickets to ignore

- `<PROJ-999>` — invalid; never match or suggest it.

## Other

- <anything else the review should always do>
