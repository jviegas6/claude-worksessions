# PROTOTYPE — throwaway

Sidebar of claude-worksessions requests and sessions; each session opens as a terminal tab.
Run: `./run.sh` (loads it into a new VS Code window without installing it).
Not for main: to be committed to a `proto/` branch once judged, then deleted here.
